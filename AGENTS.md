# 项目级协作规则

本文件是 `D:\work\miniprogram` 项目的项目级说明。后续每次进入本项目工作时，先阅读本文件，再阅读 `docs/` 中与当前任务相关的文档。

## 1. 项目定位

本项目是一个原创的家政到家服务小程序平台，参考“金职到家”的结构和业务逻辑，但不复制对方品牌、素材、代码、文案或私有接口。

系统包含：

- uni-app 小程序用户端，当前工程目录为 `miniapp/`，构建目标为微信小程序 `mp-weixin`。
- RuoYi-Vue-Plus Java 后端，当前工程目录为 `ruoyi/`。
- RuoYi-Vue-Plus 配套 Vue 3 + Element Plus 管理后台，当前工程目录为 `ruoyi-ui/`。
- MySQL 8 数据库。
- 阿里云 OSS 文件存储。

## 2. 已确认业务范围

第一版必须包含：

- 首页。
- 找阿姨/服务人员。
- 发布需求。
- 后台审核需求。
- 后台推荐阿姨。
- 用户端查看推荐阿姨。
- 预约面试。
- 合同查看。
- 服务订单。
- 团购商品。
- 单独购买。
- 发起拼团。
- 加入别人拼团。
- 团购订单。
- 系统消息。
- 我的页面。
- 管理后台。

服务分类全部保留：

- 月嫂。
- 保姆。
- 育婴师。
- 居家养老。
- 保洁师。
- 钟点工。
- 成长陪伴师。
- 家电清洗师。

## 3. 第一版明确不做

- 不接真实微信支付，先做模拟支付。
- 不做真实退款。
- 不做电子签章。
- 不做在线聊天。
- 不做服务人员独立登录端。
- 不做多管理员角色权限。
- 不做分销、返佣、会员等级。

如果用户后续没有明确改变范围，不要主动加入这些复杂功能。

## 4. 核心业务规则

### 4.1 服务人员

- 服务人员由平台管理员在后台录入。
- 小程序用户只能浏览、收藏、分享、预约面试。
- 第一版服务人员不登录、不接单、不改订单状态。

### 4.2 发布需求

- 用户发布需求后，状态为“审核中”。
- 管理员审核通过或拒绝。
- 审核通过后，管理员可以给该需求推荐一个或多个阿姨。
- 用户在“我的需求详情”里看到后台推荐阿姨列表。
- 用户可以从推荐列表进入阿姨详情并预约面试。

### 4.3 预约面试

- 用户在阿姨详情页提交预约面试。
- 后台生成预约记录。
- 管理员线下联系用户并修改预约状态。
- 第一版不做在线聊天。

预约状态：

- 待处理。
- 已联系。
- 已安排。
- 已完成。
- 已取消。

### 4.4 合同

- 合同由管理员后台创建并上传文件。
- 合同文件可以是 PDF 或图片。
- 用户在合同页查看合同状态和文件。
- 第一版不做真实电子签。

合同状态：

- 已签署。
- 已终止。

### 4.5 团购

- 团购商品支持单独购买和拼团购买。
- 用户可以发起拼团。
- 用户可以加入别人正在拼的团。
- 商品详情页要展示正在拼团的人和还差几人成团。
- 满员后拼团成功。
- 第一版支付使用模拟支付。

拼团状态：

- 拼团中。
- 拼团成功。
- 拼团失败。
- 已取消。

团购订单状态：

- 待分享。
- 待使用。
- 已使用。
- 到期。
- 售后。

## 5. 文档使用规则

重要文档：

- `docs/00-project-requirements-overview.md`：项目总需求说明书，总 TODO 和总验收标准。
- `docs/01-project-preparation.md`：环境、软件、账号准备。
- `docs/02-requirements.md`：小程序端和后台端需求。
- `docs/03-business-flows.md`：业务流程和状态。
- `docs/04-architecture.md`：系统架构和模块边界。
- `docs/05-database-design.md`：数据库表设计。
- `docs/06-api-design.md`：接口设计。
- `docs/07-admin-requirements.md`：管理后台需求。
- `docs/08-development-plan.md`：主开发计划和可打勾任务。
- `docs/superpowers/plans/2026-05-27-home-service-platform.md`：可执行开发计划。

当前本地开发端口：

- 后端：`http://localhost:8081/`，因为本机 `8080` 已被其他 Node 服务占用。
- 管理后台：`http://localhost:8082/`。
- MySQL 数据库：`ry-vue`，本地账号 `root/root`。
- Redis：本地无密码，`application-dev.yml` 已移除 Redis 密码配置。
- 开发环境验证码：`dev` 配置暂时关闭，方便本地验证后台登录；上线前必须重新开启或按正式安全策略配置。

每次开始开发前：

1. 先读本文件。
2. 再读 `docs/08-development-plan.md`。
3. 根据当前任务读取对应需求、数据库或接口文档。
4. 不要跳过计划直接写代码。

## 6. 完成任务后的强制要求

每完成一个任务，必须先测试，再打勾。

流程：

1. 完成代码或文档修改。
2. 运行与任务相关的测试或验证命令。
3. 确认结果通过。
4. 更新 `docs/08-development-plan.md` 中对应 TODO，把 `- [ ]` 改成 `- [x]`。
5. 如果该任务也在 `docs/00-project-requirements-overview.md` 或 `docs/superpowers/plans/2026-05-27-home-service-platform.md` 中出现，也同步打勾。
6. 在最终回复里说明完成了哪些任务、跑了哪些验证、哪些 TODO 已打勾。

不能在没有验证的情况下打勾。验证失败时不要打勾，要记录失败原因和下一步。

## 7. 开发注意事项

- 先做后台和数据库，再做小程序页面，因为小程序依赖后台录入的数据。
- 每个阶段只做计划内功能，不顺手扩展未确认功能。
- 不要把目标小程序的图片、Logo、品牌文案直接放进项目。
- 文件上传统一走后端，由后端上传到阿里云 OSS，前端不保存 OSS 密钥。
- 管理后台第一版只做一个管理员角色，不设计复杂权限。
- 接口返回统一结构，避免小程序和后台各写一套响应格式。
- 状态流转必须和文档一致，不要临时发明状态名。
- 数据库字段和接口字段要优先参考 `docs/05-database-design.md` 和 `docs/06-api-design.md`。
- 如果发现需求文档和实现计划冲突，先停下来说明冲突，再修改文档或请求确认。

## 8. 推荐开发顺序

1. 项目初始化。
2. 数据库与基础数据。
3. 管理员登录。
4. 服务分类。
5. 服务人员管理。
6. 文件上传。
7. 小程序首页和找阿姨。
8. 需求发布和审核。
9. 推荐阿姨。
10. 预约面试。
11. 合同。
12. 服务订单。
13. 团购商品。
14. 拼团和团购订单。
15. 内容管理和系统消息。
16. 整体联调和真机测试。
